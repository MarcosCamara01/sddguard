Execute the /spec-conflicts command defined in .sdd/workflow.md.

Use exact paths from the "Components Affected" table's `Exact path` column. Do not infer conflicts from prose or notes. Any exact path listed by two or more active specs with write roles is a potential conflict — report path, specs, roles, and a sequencing recommendation. Detection only; do not modify any spec.
