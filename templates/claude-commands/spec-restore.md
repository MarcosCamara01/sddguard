Execute the /spec-restore command defined in .sdd/workflow.md.

Restore a spec folder from a snapshot in .sdd/snapshots/. List available timestamps, confirm the target with the user, then copy the snapshot files over 1-requirements.md, 2-plan.md, 3-tasks.md. Report what was restored and what was lost.

Feature [and timestamp]: $ARGUMENTS
